# MarkHub 环境变量示例文件
# 复制此文件为 .env 并填入您的实际值

# 客户端完整性检查密钥 - 用于基本验证
# 这个密钥会暴露给客户端，仅用于基本验证
# 建议使用至少32个字符的强随机字符串
# 可以通过以下命令生成: openssl rand -base64 32
NEXT_PUBLIC_INTEGRITY_KEY=your-public-integrity-key-here

# 服务端安全密钥 - 用于强验证和数据加密
# 这个密钥仅在服务端使用，永远不会暴露给客户端
# 必须使用至少32个字符的强随机字符串
# 可以通过以下命令生成: openssl rand -base64 32
SERVER_SECRET_KEY=your-server-only-secret-key-here
